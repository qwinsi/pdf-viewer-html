{
  "name": "pdf-viewer-html",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "rimraf dist && cp -R src dist",
    "build": "bun build --entrypoints src/script.js --outdir ./dist/",
    "deploy": "gh-pages -d src"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "idb-keyval": "^6.2.1"
  }
}
